Drop your app screenshot images here (PNG or JPG).

Expected filenames:
  dashboard.png   — Dashboard / home screen
  checkin.png     — Check In / Out screen
  reports.png     — Reports screen
  prereg.png      — QR pre-registration (optional)

Recommended size: 1280px wide or similar 16:10 aspect ratio.

After adding files, refresh the dev server (npm run dev).
